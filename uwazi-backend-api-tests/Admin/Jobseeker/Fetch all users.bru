meta {
  name: Fetch all users
  type: http
  seq: 1
}

get {
  url: {{base_url}}/admin/users?pageNumber=1&activeTab=all
  body: none
  auth: bearer
}

params:query {
  pageNumber: 1
  activeTab: all
  ~query: stanley
}

auth:bearer {
  token: {{auth_token}}
}
